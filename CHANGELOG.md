# 更新日志

## v1.1.0

### 新增：我的资料（本地知识库）

设置里多了一个「我的资料」。把文件或文件夹加进去，它回答前会先在你的资料里
查一遍，把最相关的几段一起读，并在回答里标出依据来自哪份文件。支持 txt、md、
PDF 和各种代码、配置文件。资料只存在本机。

**为什么加这个。** 27B 的模型对冷门事实记性很差。我们做了一组对照：同一个问题、
同一个随机种子，唯一变量是"有没有查资料"。

| 问题 | 不查资料 | 查资料 |
|---|---|---|
| CVE-2018-8120 是哪个组件的漏洞 | 「Apache Tomcat 的路径遍历漏洞」（编的，真身是 win32k.sys 空指针提权） | 「win32k.sys，属于本地权限提升漏洞」 |
| SeImpersonatePrivilege 配合哪些工具 | 列了 runas、psexec，Potato 家族一个没提 | GodPotato / JuicyPotato / RoguePotato / PrintSpoofer / SweetPotato |

检索有两档。默认那档是关键词检索，**不需要任何额外下载**；打开「更懂意思的检索」
会多下一个 610 MB 的语义模型（只下一次），让换了说法也能查到。

### 改变：去拒答适配器改成由预设决定

以前它无条件生效。现在只有「默认」不带它 —— 那个预设存在的意义就是"最接近模型
原本的样子"。其余预设照旧都带。

### 撤销：红队与反编译预设

本来准备做两个专业方向的预设。做完对照实验后放弃了。

我们拿 31 个「懂行的人必须提到的点」去考五个安全方向的适配器组合（其中一个是
我们自己转的、256/256 层全覆盖的红队 LoRA）。结果：

- 覆盖率最高的一组只有 **35.5%**；
- `SeImpersonate`、`AlwaysInstallElevated`、`tcache`、`fastbin`、`safe-linking`、
  `AmsiScanBuffer`、`amsiInitFailed`、`.user.ini` 这些关键词，**五个组合一次都没出现**；
- 同时它们编造了大量不存在的东西：把 `CVE-2018-8120` 说成 wininet.dll 缓冲区溢出、
  把 `CVE-2019-14832`（实为 Moodle 漏洞）当成 Windows 提权、发明
  「PrintNightmare 2.0/3.0」和 `CVE-2023-38816`、发明 Metasploit 模块
  `exploit/windows/local/wininet_rce`、在 P/Invoke 里写不存在的 `FreeVirtualMemory`。

反编译那个（Apache-2.0，237 MB）同样没赚到：23 个考点上覆盖率 78.3%，低于
「只挂破限」的 82.6%，而且把 PowerPC 专有指令认成了 MIPS。

结论很直接：**适配器能改的是"愿不愿意说"和"什么语气"，改不了"知不知道"。**
在专业领域，一个愿意说但不知道的模型比一个直接拒答的模型更危险 —— 它会把编造
的内容包装得像真的。所以这两个预设换成上面的资料检索。

### 修复

- `--data-dir` 只换了写盘位置，从来没读过那个目录里的设置（调试时表现为界面里
  的令牌和磁盘上的对不上）。
- 打包配置把 `numpy` 排除掉了，而它给预设调权重时是必需的 —— 源码全绿，只有
  打包版选非 1.0 权重的预设才会崩。
- 打包依赖图会顺着 `gguf/vocab.py` 函数体里的一个 `import transformers` 把整个
  site-packages 卷进来，导致构建崩溃。已挡住，并加了两道闸门：exe 自检
  （`--selftest`）和体积上限。
- 管道输出按本地代码页写，打包脚本读 `--selftest` 的结果是乱码（明明全过，
  看起来像一堆问号）。

### 其它

- `BonsaiLocal.exe` 从 35.8 MB 变成 51.5 MB（多了 numpy 与 pypdf）。
- 引擎包换成多架构 CUDA 构建（sm_75 到 sm_120），不再只支持 40 系。
- 新增 `BonsaiLocal.exe --selftest`，打包流程每次会自动跑一遍。

## v1.0.0 — 2026-09-25

第一个公开版本。

双击就能用的本地大模型。没有账号，没有 API Key，对话不出这台电脑。

### 怎么开始

1. 下载下面的 `BonsaiLocal.exe`，放到任意文件夹（建议单独放一个）。
2. 双击它。第一次会显示下载进度。
3. 下载完就能聊了。

不需要装 Python、CUDA 或任何运行库。程序会自动识别显卡，下载配套的推理引擎。

### 首次运行会下载什么

| 内容 | 大小 |
|---|---|
| 模型 | 5.5 GB（只下一次，断了会从断点续传） |
| 推理引擎（有 N 卡） | 约 606 MB |
| 推理引擎（无独显） | 约 8 MB |

### 自带的功能

- **聊天界面**：深色，开箱即用，对话记录留在本机。
- **手机也能用**：设置 → 远程访问，打开开关会得到一个临时网址和二维码（走 Cloudflare 免费通道，不用注册）。
- **能被程序调用**：自带 OpenAI 兼容接口，令牌在设置页复制。

### 安全边界

远程访问只暴露应用层：**设置接口对外网一律拒绝**，每个模型请求都必须带令牌。
公网页面里不会包含令牌。

### 关于这个模型

底座是 PrismML 的三值 Bonsai 2 27B（Apache-2.0），本程序叠加了一个 8 MB 的秩-1
适配器，去掉了它原本的拒答行为。**也就是说，它会回答底座模型本来会拒绝的请求。**
生成内容的准确性与合法性由使用者负责。

### 校验

`SHA256SUMS` 附在下面。杀毒软件可能因为单文件打包而误报。

### 已知问题

- 可执行文件没有代码签名，Windows SmartScreen 会拦一次。
- 引擎包偏大（606 MB），主要来自 CUDA 运行时。换成 Vulkan 构建可以降到约 30 MB，
  并同时支持 AMD 与核显，计划在后续版本处理。
